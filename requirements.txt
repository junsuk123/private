# Runtime dependencies for OBAITS (Ontology Based AI Trading System).
# Mirrors [project].dependencies in pyproject.toml.
# Install with: pip install -r requirements.txt
fastapi
httpx
numpy
owlrl
pyshacl
PyYAML
rdflib
starlette>=0.40.0,<0.47.0
tzdata
uvicorn
websockets

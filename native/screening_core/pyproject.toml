[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "screening_core"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
